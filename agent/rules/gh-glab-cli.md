When fetching issues and such from GitHub/GitLab, always use the `gh-1p` and `glab-1p` commands,
they are wrapper scripts around the GitHub and GitLab CLI, respectively, that use the 1Password
CLI for auth.
